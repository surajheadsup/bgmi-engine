<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="?attr/colorSurfaceContainerHigh"
    android:paddingStart="6dp"
    android:paddingEnd="6dp"
    android:paddingTop="8dp"
    android:paddingBottom="8dp"
    android:layout_marginBottom="1dp">

    <!-- Select checkbox -->
    <CheckBox android:id="@+id/cbProcSelect"
        android:layout_width="20dp" android:layout_height="20dp"
        android:buttonTint="@color/accent" android:scaleX="0.75" android:scaleY="0.75" />

    <!-- Name + tag -->
    <LinearLayout android:layout_width="0dp" android:layout_height="wrap_content"
        android:layout_weight="1" android:orientation="horizontal" android:gravity="center_vertical"
        android:layout_marginStart="6dp">

        <TextView android:id="@+id/tvProcName" android:layout_width="0dp" android:layout_height="wrap_content"
            android:layout_weight="1" android:textColor="?attr/colorOnSurface" android:textSize="12sp"
            android:ellipsize="end" android:singleLine="true" />

        <TextView android:id="@+id/tvProcTag" android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:textSize="8sp" android:textStyle="bold" android:visibility="gone"
            android:layout_marginStart="4dp"
            android:background="@drawable/mode_badge_bg"
            android:paddingStart="4dp" android:paddingEnd="4dp"
            android:paddingTop="1dp" android:paddingBottom="1dp" />
    </LinearLayout>

    <!-- Memory -->
    <TextView android:id="@+id/tvProcMemory" android:layout_width="42dp" android:layout_height="wrap_content"
        android:textColor="?attr/colorOnSurface" android:textSize="11sp" android:textStyle="bold" android:gravity="end" />

    <!-- Whitelist button -->
    <TextView android:id="@+id/btnProcWhitelist" android:layout_width="28dp" android:layout_height="28dp"
        android:text="+" android:textColor="?attr/colorOutline" android:textSize="14sp"
        android:gravity="center" android:background="@drawable/toggle_btn_bg" />
</LinearLayout>
